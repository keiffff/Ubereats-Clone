import React from 'react';
import { Image } from 'react-native';
import { styles } from './styles';

export const LogoutIcon = () => <Image source={require('assets/shutdown-icon.png')} style={styles.menuIcon} />;
